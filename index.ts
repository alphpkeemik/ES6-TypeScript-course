/**
 * Pick
 * Omit
 * Partial
 * Required
 * Record
 */

type TCar = {
    make: string,
    model: string
    year: number
}

const car: TCar = {make: 'ford', model: 'focus', year: 2006}